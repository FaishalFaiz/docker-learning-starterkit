// Ini adalah Class (blueprint)
class Mobil {
  // Properti akan kita tambahkan nanti
}

void main() {
  // Ini adalah Object (instance dari class Mobil)
  var mobilAvanza = Mobil();
  var mobilBrio = Mobil();

  print(mobilAvanza); // Output: Instance of 'Mobil'
  print(mobilBrio);
}
